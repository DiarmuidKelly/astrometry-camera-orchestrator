# Runtime dependencies for camera-orchestrator.
# The Canon EDSDK (libEDSDK.so) is NOT a pip package -- install it separately
# and point EDSDK_LIB_PATH / config at it.
numpy>=1.24
opencv-python>=4.8
astropy>=6.0
PyYAML>=6.0
exifread>=3.0
pydantic>=2.0

gphoto2>=2.6
