"""Domain layer — contracts and pure logic.

Value objects (models), ports (abstract interfaces), pure calculations, and
errors. Imports nothing external (no gphoto2, cv2, docker, astropy, argparse):
every other layer depends inward on this one.
"""
