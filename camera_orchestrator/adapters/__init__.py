"""Adapters — concrete implementations of the domain ports.

The only layer where external libraries live (gphoto2, cv2, astropy, docker,
subprocess, exifread). Each adapter implements a port from domain.ports.
"""
